@model Lab5.Models.LabModel

<style>
    body {
        font-family: 'Arial', sans-serif;
        background-color: #eaeaea;
        margin: 0;
        padding: 0;
    }

    .container {
        width: 80%;
        margin: 20px auto;
        background-color: #f3f3f3;
        padding: 20px;
        border-radius: 15px;
        box-shadow: 0 0 30px rgba(0, 0, 0, 0.2);
        text-align: left;
    }

    h1, h2 {
        color: #333;
    }

    p {
        color: #666;
        font-size: 18px;
    }

    label {
        font-weight: bold;
        color: #555;
        display: block;
        margin-bottom: 10px;
    }

    input[type="text"],
    textarea {
        width: 100%;
        padding: 12px;
        margin: 8px 0;
        border: 2px solid #ddd;
        border-radius: 8px;
        box-sizing: border-box;
    }

    input[readonly] {
        background-color: #ecf0f1;
        cursor: not-allowed;
    }

    .btn {
        background-color: #4CAF50;
        border: none;
        color: white;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        font-size: 18px;
        margin: 12px 0;
        cursor: pointer;
        padding: 15px 30px;
        border-radius: 8px;
        transition-duration: 0.4s;
        font-weight: bold;
    }

        .btn:hover {
            background-color: #45a049;
        }

    .form-container {
        text-align: center;
    }

    .form-group {
        margin-bottom: 25px;
    }
</style>

<div class="container">
    <h1>Лабораторна робота N3</h1>
    <h2>Умова задачі:</h2>
    <p>
        Тезею з лабіринту Мінотавра допоміг вийти клубок ниток. Ви можете
        замість клубка використовувати персональний комп'ютер.
        Потрібно написати програму, яка вводить маршрут Тезея в лабіринті і
        знаходить найкоротший зворотний шлях, яким Тезей зможе вийти з лабіринту,
        не заходячи в глухий кут і не роблячи петель.
    </p>

    <h2>Вхідні дані:</h2>
    <p>
        Вхідний файл INPUT.TXT містить маршрут Тезея, який представлений
        рядком, що складається з літер: N, S, W, E та довжиною від 1 до 200.
        Літери означають:
        N - один "крок" на північ, S - один "крок" на південь, W - один "крок" на захід,
        E - один "крок" на схід.

    </p>

    <h2>Вихідні дані:</h2>
    <p>
        У вихідний файл OUTPUT.TXT записується аналогічно до вхідного файлу
        знайдений зворотний шлях. Якщо маршрут неоднозначний, слід вибирати
        відповідно до наступного пріоритету: N, E, S, W.
    </p>

    <h2>Приклад</h2>
    <p>
        <b>INPUT:</b><br>
        EENNESWSSWE <br />
        <br />
        <b>OUTPUT:</b><br> NWW
    </p>
    <form asp-controller="Labs" asp-action="Calculate" method="post">
        <div class="form-group">
            <label for="Input">Введіть дані:</label>
            <input asp-for="LabNumber" type="hidden" value="3" />
            <textarea asp-for="Input" class="form-control" id="Input" rows="7"></textarea>
        </div>
        <button type="submit" class="btn">Обчислити</button>
    </form>
    <div class="form-group">
        <label for="Output">Результат:</label>
        <textarea asp-for="Output" class="form-control" id="Output" readonly rows="7"></textarea>
    </div>
</div>

